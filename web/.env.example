# Keep this directory outside the application source tree so upgrades do not touch your library.
DATA_DIR=../shentuji-data

# Set to 1 when the app is served through HTTPS.
COOKIE_SECURE=0

# Optional upload limit in megabytes (default: 50).
MAX_IMAGE_MB=50
